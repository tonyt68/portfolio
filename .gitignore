# Secrets & Credentials
.env
.env.*
*.pem
*.key
*.crt
*.p12
*.pfx
service-account.json

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform.lock.hcl
*.tfvars
*.tfvars.json

# Certificates
certs/

# CA key material (agent-sso-catalog's own CA — different literal name from
# certs/ above, not covered by that rule; see docs/agent-sso-catalog-design.md)
ca/

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# Node (tab-deep-link.js unit tests only — no build step for the site itself)
node_modules/
pnpm-lock.yaml
package-lock.json
pnpm-workspace.yaml

# Demo videos — hosted on S3/CloudFront now, see portfolio-video-infra/
*.mp4

# Local only — not for portfolio repo
last-mile-zero-trust/
ietf-a2a-trust-poc/
sentinel-rebac-poc/
sentinel-fips/
sentinel-mcp-poc/
rag-knowledge-assistant/
portfolio-video-infra/
hack-my-own-code/
a2a-trust-playground/
sentinel-mesh-zero-trust/
zero-trust-payments/
hitl/
agentic-obo-okta/
aws-obo-permissions/
obo-as-a-platform/
okta-auth0-identity-broker/
autonomous-code-reviewer-agent/
awshound/
backup.sh
SHIFT_LEFT.md
.claudeignore
portfolio.py
requirements.txt
TonyAI-Rebranding.md
*.original.md
sentinel-agent-drift-scanner/
sentinel-triage-agent-harness/
hmoc-demo-target/
hmoc-demo-vuln-target/
ui-check-out/
c++/
RAGvsAgenticRAGvsGraphRAG.rtfd/
backup-keys.sh
website-robot.png
*.bak

# Biz/idea design docs — source of truth lives in ~/biz/ideas/, never committed
# to any repo (contains non-compete analysis, client names, interview-IP claims)
idea-*.md
idea-governed-writeback-gateway.md
governed-writeback-gateway/

# Other local-only PoC directories at ~/dev root — each is its own future repo
# (see pocs-to-github-plan), not part of the portfolio site's own repo. Found
# completely unprotected during a pre-push safety check, 2026-09-11 -- neither
# was covered by any existing rule, only accidentally untracked so far.
agent-sso-catalog/
template-vending-machine/

# Portfolio tech-debt register — NEVER commit (catalogs vulns in published solutions)
tech-debt/

# agent-sso-catalog runtime state (start.sh/stop.sh) — PIDs and logs,
# never anything to commit
run/
logs/
