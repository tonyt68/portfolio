apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sentinel-quarantine
spec:
  podSelector:
    matchLabels:
      app: redis
  policyTypes:
  - Egress
  egress: [] # This means "Allow nothing out"
